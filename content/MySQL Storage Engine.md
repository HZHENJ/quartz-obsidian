%% [[MySQL]] %%
MySQL常见的存储引擎有三种：
1. InnoDB
2. MyISAM
3. MEMORY

InnoDB存储数据的原理：InnoDB的数据是按照**数据页**为单位来读写的，数据页默认大小是16KB（数据库的IO操作的最小单位是数据页，意味着数据库每次读写都是以16KB为单位，一次最少从磁盘中读取16KB的内容到内存中，**一次最少把内存中的16KB内容刷新到磁盘中**）