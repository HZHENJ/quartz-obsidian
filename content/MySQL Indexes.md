%% [[MySQL]] %%
索引能让MySQL快速定位数据，避免全表扫描，在MySQL的InnoDB中使用[[MySQL B+ Tree|B+ 树]]作为索引。除了提高查询效率，索引还能加速排序、分组、连接等操作（？）

索引的分类：
- 数据结构
- 物理结构
- 字段特性
- 字段数量

InnoDB采用B+树作为索引的原因：

索引失效的情况：（为什么）

主键索引

二级索引

回表

MRR